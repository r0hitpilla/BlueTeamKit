input {
  beats {
    port => 5044
  }
}
filter {
  # you can add grok rules here
}
output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "bluetest-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
